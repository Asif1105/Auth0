import { makeStyles } from '@mui/styles';
import { DLSTheme } from '@shop/dls/src/types';

export const useWidgetsWrapperStyles = makeStyles((theme: DLSTheme) => ({
  container : {},
  item      : {}
}));
